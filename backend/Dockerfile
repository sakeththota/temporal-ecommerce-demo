FROM golang:1.24-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY backend/ backend/

RUN CGO_ENABLED=0 go build -o /app/bin ./backend/cmd/api

FROM alpine:3.20

COPY --from=builder /app/bin /app/bin

ENTRYPOINT ["/app/bin"]
